import { styled } from "@mui/system";

export const LandingPageStyles = styled('div')({
  width: '100%',
  position: 'relative',
  bottom: '0',
  padding: '0 !important',
  margin: '0 !important',
  minHeight: 400,
})