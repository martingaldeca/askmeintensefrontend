import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const GeneralBottomNavigationStyled = styled(Box)({
  width: '80%',
  height: '55px',
  position: 'fixed',
  bottom: 0,
  left: '10%',
});
